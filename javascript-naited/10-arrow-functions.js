function doubleNumber(number) {
  return number * 2;
}

console.log(doubleNumber(5));

const double = (number) => {
  return number * 2;
};

console.log(double(5));

const doubleShort = number => number * 2;

console.log(doubleShort(5));

const names = ["Mari", "Jüri", "Kati"];

names.forEach((name) => {
  console.log(name);
});